export {};

enum Months {
  January,
  February,
  March,
}

console.log(Months.January);

enum Week {
  Sunday = 1,
  Monday,
}
console.log(Week.Monday);

enum COLORS {
  RED = '#FF0000',
  WHITE = '#FFFFF',
  //   YELLOW = '#FFFF00',
}

enum COLORS {
  YELLOW = '#FFFF00',
  // GRAY
}

console.log(COLORS.RED);
